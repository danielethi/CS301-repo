//a function that returns the sum of the first and the last element in the array
function addend(arr) {
      let x=arr[0];
      let y=arr[arr.length-1];
      console.log(x+y);


}
addend([10,2,3,5]);
